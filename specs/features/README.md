# Feature Specs

这里存放 `text2sql` 的单功能规约。

创建新功能时：

1. 从 `specs/templates/feature-spec-template.md` 复制一份到本目录。
2. 文件名使用 kebab-case，例如 `natural-language-to-sql.md`。
3. 先完成需求澄清并保持 `Status: Draft`。
4. 用户确认后将状态改为 `Accepted`，再进入实现。
5. 实现和验证完成后将状态改为 `Implemented`。
